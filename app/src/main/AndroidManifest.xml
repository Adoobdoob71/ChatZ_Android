<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chatz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".SettingsScreen"></activity>
        <activity android:name=".ProfileScreen" />
        <activity android:name=".Register" />
        <activity android:name=".PrivateChatWindow" />
        <activity android:name=".ContactScreen" />
        <activity android:name=".CreatePM_Activty" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".AddPost" />
        <activity android:name=".AddComment" />
        <activity android:name=".Login" />
        <activity android:name=".PostScreen" />
        <activity android:name=".ChatWindow" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>